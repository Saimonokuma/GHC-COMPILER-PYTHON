## Queued Improvements (2025-05-08)
- [ ] Investigate replacement of `os.pathsep.join` and strings with `pathlib.Path` list manipulations in `_sterilize_environment`.
- [ ] Migrate `tests/test_e2e.py` setup to use hatch/pip install rather than relying on path hacks to find wrappers.
