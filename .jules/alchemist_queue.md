## Queue

- Explore potential for further O(n) improvements if nested loops are found.
- Consider utilizing `match/case` structural pattern matching more pervasively in the codebase.
- Look into using `itertools` for cleaner iterative logic.
