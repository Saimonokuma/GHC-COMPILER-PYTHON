## Potential Future Optimizations

- [ ] Look into potential C-extensions or Rust/Zig rewrites for the path replacement logic, avoiding regex overhead entirely.
- [ ] Evaluate caching mechanisms for `_resolve_binary` lookup across multiple subprocess wrapper invocations.
